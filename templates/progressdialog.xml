<?xml version="1.0" encoding="UTF-8" ?>
<document>
  <head>
    <style>
    </style>
  </head>
  <productBundleTemplate>
    <shelf centered="true">
      <section>
        <lockup disabled="true">
          <img src="" width="1200" height="600" />
          <overlay>
	        <text style="tv-position: top; margin: 10; text-align: center; font-size: 50; color: rgb(0,0,0);">{{title}}</text>           
            <progressBar style="tv-position: center;" id="progress" msgid="{{msgid}}" documentURL="{{url}}" value="{{value}}" />
            <text id="text" style="tv-text-max-lines: 3; margin: 10; text-align: center; font-size: 30; color: rgb(0,0,0);">{{text}}</text>
          </overlay>
        </lockup>
      </section>
    </shelf>
  </productBundleTemplate>
</document>