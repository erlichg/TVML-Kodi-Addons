<?xml version="1.0" encoding="UTF-8" ?>
<!--
Copyright (C) 2016 Apple Inc. All Rights Reserved.
See LICENSE.txt for this sample’s licensing information
-->
  <document>
	<head>
	  <style>
	  </style>
	</head>
	<listTemplate>
	  <list>
		  <section>
				{% for item in menu %}
				{% if msgid is defined %}
				<listItemLockup menuURL="{{item.menuurl}}" onselect="notify('/response/{{msgid}}/{{loop.index}}'); load('{{url}}')">
				{% else %}
				<listItemLockup menuURL="{{item.menuurl}}" onselect="load('/catalog/{{plugin.name | base64encode}}/{{item.url | base64encode}}')">								
				{% endif %}
				 	<title>{{item.title}}</title>
				 	<relatedContent>
					 	<lockup>
						 	<img src="{{item.icon}}" width="857" height="842"/>
						 	<title>{{item.subtitle}}</title>
						 	<description>{{item.details}}</description>
					 	</lockup>
				 	</relatedContent>			 					 	
				</listItemLockup>
				{% endfor %}
			</section>
	  </list>
	</listTemplate>
  </document>
